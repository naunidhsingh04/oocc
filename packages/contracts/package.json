{
  "name": "oocc-contracts-schemas",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Canonical JSON Schemas for the OOCC trace and viz-plan contracts, plus the codegen script that projects them into packages/contracts/python and packages/contracts/ts. See docs/PRD.md §3.1-3.3 and §4.3. This package is append-only and shared: neither person changes it alone.",
  "scripts": {
    "gen:contracts": "node scripts/generate.mjs",
    "lint": "echo 'no lint target in packages/contracts'",
    "typecheck": "echo 'no typecheck target in packages/contracts'",
    "test": "echo 'no test target in packages/contracts (see packages/contracts/python and packages/contracts/ts)'"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^15.0.4",
    "prettier": "^3.4.2"
  }
}
