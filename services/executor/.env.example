# services/executor environment variables.
#
# The executor is intentionally self-contained: no database, no cache, no
# outbound calls to anything else — it only ever receives HTTP requests
# from apps/api. The only variable that matters is the port it binds to.

# Port this service listens on. Render sets this automatically — you
# normally don't need to set it yourself on Render.
PORT=8001
